apiVersion: v1
kind: Service
metadata:
  name: {{ include "myapi-helmchart.fullname" . }}
  labels:
    {{- include "myapi-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myapi-helmchart.selectorLabels" . | nindent 4 }}
